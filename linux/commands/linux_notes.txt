https://www.kernel.org/doc/Documentation/networking/bonding.txt
https://docs.redhat.com/en/documentation/red_hat_enterprise_linux/7/html/networking_guide/sec-infiniband_and_rdma_related_software_packages
https://docs.redhat.com/en/documentation/red_hat_enterprise_linux/7/html/networking_guide/sec-tranferring_data_using_roce
https://opensource.com/article/19/10/strace
https://docs.kernel.org/userspace-api/netlink/intro.html
https://docs.kernel.org/networking/netconsole.html
https://www.snia.org/technology-communities/sff/specifications


lspci -d 1924: -vv
lspci -d 1924: -v
lspci -d ::0200 (for all ethertnet ports)
mount /runbench-boot

Create docker group on host:
	sudo groupadd docker -og 10519
	echo 'docker:x:10519:' | sudo tee -a /etc/group
Add user (xcb-jenkins-rb) to the docker group
	xcb-jenkins-rb@dellr640q:/home/prku$ sudo usermod xcb-jenkins-rb -G docker

Path for PCI address: /sys/bus/pci/devices/0000

To add user into a group:
	sudo usermod xcb-jenkins-rb -G docker
To add a groupto a host:
	echo 'docker:x:10519:' | sudo tee -a /etc/group
To check the port status for SSH:
	netstat -putan |grep ssh
To SSH service is running:
	sudo systemctl status sshd
To start SSH service:
	sudo service ssh start
To check the SSH log:
	sudo vim /var/log/auth.log
To run only bash_profile
	A login shell is used so bash_profile runs but not bashrc

To view documents on installed OS/distro look at:
	/usr/share/doc/
	e.g: /usr/share/doc/rdma-core/tests

To set command line arguments to wrap properly:
	shopt | grep checkwinsize (if output is not "checkwinsize    on")
	shopt -s checkwinsize (to set the option)
	shopt -u checkwinsize (to deactivate the option)
To check LDAP configuration:
	ldapsearch -xLLL -s base dn (output: dn: dc=amdldap,dc=com)

To change a directory's permission to "drwxr-sr-x": chmod u=rwx,g=rx+s,o=rx /path/to/dir

To capture RDMA packets: ethtool --set-priv-flags enp68s0 rdma-sniffer on; tcpdump -i enp68s0 -s0 -w pingpong.pcap

To see haedware topology: lstopo
To see virtual machines (VM): virsh list
To search utility in yum repository: yum whatprovides ibv_devinfo

TO connect UART using minicom: sudo minicom -D /dev/ttyUSB0 . Default baud 115200 is fine.  Disable hw flow control in settings.

devlink dev flash pci/0000:0a:00.0 file xilinx/x3/x3522-bundle.update

If the module is not in depmod (i.e. you used insmod to insert it), you have to remove it with rmmod
Path to check module parameters: /sys/module/<module_name>/parameters

tar -xvf aux_bus_srpm.tgz
	Extracted `logfile_link` and `auxiliary-1.0.1.0-1.src.rpm`
 rpmbuild --rebuild auxiliary-1.0.1.0-1.src.rpm
	Created `rpmbuild` directory structure and put rpm file in `rpmbuild/RPMS/x86_64/`
 sudo rpm -ivh /home/prku/rpmbuild/RPMS/x86_64/kernel-module-auxiliary-RHEL8-4.18.0-240.el8.x86_64-1.0.1.0-1.x86_64.rpm
	Installed `kernel-module-auxiliary-RHEL8-4.18.0-240.el8.x86_64-1.0.1.0-1.x86_64` RPM package
 make srpm (on xcbbldcent01)
	Created `rpm` directory structure and put the source tar file in `rpm/SOURCES`, spec file in `rpm/SPECS` and source rpm in `rpm/SRPMS`
 rpmbuild --rebuild xilinx-efct-1.2.3.0.2.g30e5100-1.src.rpm
	Created `kernel-module-xilinx-efct--4.18.0-240.el8.x86_64-1.2.3.0.2.g30e5100-1.x86_64.rpm` file and placed it at `rpmbuild/RPMS/x86_64/`
 sudo rpm -ivh /home/prku/rpmbuild/RPMS/x86_64/kernel-module-xilinx-efct--4.18.0-240.el8.x86_64-1.2.3.0.2.g30e5100-1.x86_64.rpm
	Installed `kernel-module-xilinx-efct--4.18.0-240.el8.x86_64-1.2.3.0.2.g30e5100-1.x86_64` RPM package

To build portable binary with static and dynamic library: LDFLAGS='-Wl,-Bstatic -liperf -Wl,-Bdynamic -lm -lc' make
To build portable iperf3 binary with static and dynamic library: ./configure --enable-static --disable-shared; make; make install
To clean object file created by "make" command: make clean
To uninstall the executable installed by "make install": make uninstall
To SSH without fingerprint verification: ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null dellr660d.lab.com

To create soft link: ln -s target(absolute pat of file/directory) link_name(absolute path)
To get root complex port of PCIe device: lspci -s 44: -PPvv 

To search fix string in ag: ag "\bFeatureTestRunbench\b"

To check vendor of DUT: cat /sys/class/dmi/id/sys_vendor

To run any command in every 2 seconds: watch command
To edit cronjob file: crontab -echo
cronjob time definition is: Minute(0-59) Hour(0-23) DayOfMonth(1-31) MonthOfYear(1-12) DayOfWeek(0-6; Sunday-Saturday)

To check directory exists and run && for TRUE and || for FALSE: [ ! -d /home/avi/Tecmint ] && printf '\nno such /home/avi/Tecmint directory exist\n' || printf '\n/home/avi/Tecmint directory exist\n'

To get module info from .ko in case module throws error: sudo modinfo <path_to_ko_file>/ionic_rdma.ko 

To load a Kernel Module with Custom Parameters: modprobe e1000e InterruptThrottleRate=3000,3000,3000 EEE=1
	sudo insmod ./rdma_krping.ko krping_debug=1

To restart multiple sevices simultaneously:
	cd /etc/systemd/system/multi-user.target.wants/
	systemctl restart runbench*

To check MTU size: cat /sys/class/net/enp1s0f0/mtu
To set MTU size: sudo ip link set dev enp132s0 up mtu 9000

To bring down the link: sudo ip link set dev enp132s0 down
To bring up the link with particular MTU size: sudo ip link set dev enp132s0 up mtu 9000

To find between dates and delete them: find /tmp/ -maxdepth 1 -newerat 2023-10-16 ! -newerat 2023-10-19 | grep .pcap | xargs sudo rm -f

To check power usage in micro Joules: cat /sys/class/powercap/*/energy_uj

To use awk command: ll | awk '{print $3}'
To get last field/element from awk: ll test_cases | awk '{print $NF}'

Path for core dump: /var/lib/systemd/coredump
To run gdb on core dump:  gdb ./hw ./core.hw.0.e600cbc6dc9f4ad0a6a951cbf9f8d7cf.705897.1708443907000000

To compile C code with debug info: gcc -Wall -Og -g -o hw hw.c

To decompress zstd core file: zstd -d core.hw.0.e600cbc6dc9f4ad0a6a951cbf9f8d7cf.705897.1708443907000000.zst

To SSH using identity key: ssh -Tn -i <path_to_identity_file>/ssh_root_ecdsa root@dellr7625b /rdma_utils/mpi/bin/mpirun

To check kernel log:  /var/log/kern.log

To set SIGPIPE error: set -o pipefail

How strace works: Command-line utility -> Invokes functions from system libraries (glibc) -> Invokes system calls
To check system call trace: strace <executable>
To log the output of strace to a file: strace -o trace.log <ls testdir/>
To trace any child processes created by the process currently being traced: strace -f <ls testdir>
To get the stat of system calls: strace -c <ls testdir/>
To search for particular system call: strace -e open <ls testdir> (strace -e write,getdents <ls testdir>)
To check the system call for a running PID: strace -p <PID>
To see a timestamp against all your system calls: strace -t <ls testdir/>
To know the time spent between system calls: strace -r <ls testdir/>

To check strings into binary / executable: strings <binary>

To know which functions were called from the glibc library: ltrace <ls testdir/>

To add group named docker: sudo groupadd docker
To add current user to the docker group: sudo usermod -aG docker $USER

To use time with pipe: cat fn | bash -c "time wc -l"

To configure interface: sudo ip address add dev enp106s0f0np0 local 192.168.131.32/21 broadcast 192.168.135.255
To bring up the interface: sudo ip link set enp106s0f0np0 up

To edit stream using sed: ethtool eno8303 | sed -n '/Supported link modes:/,/Supported pause frame use:/p'

To check all the supported architecture by host CPU: gcc -march=native -Q --help=target

To see the dependencies use full path of executable: ldd /usr/bin/ibv_devinfo

To work on compressed file: zless; zcat

To get the kernel function trace:
	cd /sys/kernel/debug/tracing/
	echo ib_set_device_ops > set_ftrace_filter
	echo ionic_counter_alloc_stats >> set_ftrace_filter
	echo rdma_counter_bind_qp_auto >> set_ftrace_filter
	echo alloc_and_bind >> set_ftrace_filter
	echo ionic_counter_bind_qp >> set_ftrace_filter
	cat set_ftrace_filter
	echo function > current_tracer
	echo 1 > tracing_on
	cat current_tracer

	####Run the test
	cat /sys/kernel/debug/tracing/trac

The correct order for converting source code into an executable file is: Preprocessing -> Compiling -> Assembly -> Linking.
	source file(code) -> preprocessor(processed file) -> compiler(assembly code) -> assembler(object code) -> linker(executable)
	source(.c) -> preprocessing(.i) -> compiling(.s) -> assembly(.o) -> linking(executable)
	
	1. Pre-processing (The preprocessed output is stored in the .i file)
	This is the first phase through which source code is passed. This phase includes:
	Removal of Comments
	Expansion of Macros
	Expansion of the included files.
	Conditional compilation
	
	2. Compiling
	The next step is to compile .i file and produce an intermediate compiled output file .s file. This file is in assembly-level instructions.
	
	3. Assembling
	In this phase the .s file is taken as input and turned into .o file by the assembler.
	This file contains machine-level instructions. At this phase, only existing code is converted
	into machine language, and the function calls like printf() are not resolved.
	
	4. Linking
	This is the final phase in which all the linking of function calls with their definitions is done.
	Linker knows where all these functions are implemented. Linker does some extra work also, it adds
	some extra code to our program which is required when the program starts and ends. For example,
	there is a code that is required for setting up the environment like passing command line arguments.
	This task can be easily verified by using $size filename.o and $size filename. Through these commands,
	we know how the output file increases from an object file to an executable file. This is because of the extra code that Linker adds to our program.

To see kernel logs: 
	sudo dmesg -w
	sudo journalctl --system -f
	sudo tail -F /var/log/messages (if it's available)



ip link show
ip link set dev enp69s0 mtu 9000

modprobe -r sfc


KVM:
https://host.uk.com/ (user:password)(On Internet Explorer)
 
VNC viewer:
host.uk.com:5900

rpm -q --changelog <package_name>

To check running daemon: systemctl | grep daemon
To list all open socket: ss -l;   cat /proc/net/udp;   cat /proc/net/tcp

To get all files in a directory: find . –maxdepth 1 –type f

High level code into assembly language: https://godbolt.org/

iperf3 --one-off -J -s -p 20000
iperf3 -J -c 192.168.129.178 -l 1 -t 60 -p 20000

To see the assembly code: https://godbolt.org/

Installing MLNX_OFED Using YUM
	Setting up MLNX_OFED YUM Repository
		1. Log into the installation machine as root.
		2. Mount the ISO image on your machine and copy its content to a shared location in your network.
			# mount -o ro,loop MLNX_OFED_LINUX-<ver>-<OS label>-<CPU arch>.iso /mnt
		3. Download and install Mellanox Technologies GPG-KEY:
			# wget http://www.mellanox.com/downloads/ofed/RPM-GPG-KEY-Mellanox
		4. Install the key.
			# sudo rpm --import RPM-GPG-KEY-Mellanox
		5. Check that the key was successfully imported.
			# rpm -q gpg-pubkey --qf '%{NAME}-%{VERSION}-%{RELEASE}\t%{SUMMARY}\n' | grep Mellanox
		6. Create a yum repository configuration file called "/etc/yum.repos.d/mlnx_ofed.repo" with the following content:
			[mlnx_ofed]
			name=MLNX_OFED Repository
			baseurl=file:///<path to extracted MLNX_OFED package>/RPMS (/mnt/RPMS)
			enabled=1
			gpgkey=file:///<path to the downloaded key RPM-GPG-KEY-Mellanox> (/mnt/RPM-GPG-KEY-Mellanox)
			gpgcheck=1
		7. Check that the repository was successfully added.
			# yum repolist
	
	Installing MLNX_OFED Using the YUM Tool
		1. # yum search mlnx-ofed-
		2. Install the desired group.
			# yum install mlnx-ofed-all
			
	To check RPM GPG keys:
		$ rpm -qa gpg-pubkey*
		To get the information on a key:
			rpm -qi gpg-pubkey-2f86d6a1-5cf7cefb (gpg key ID) 

To fix grub mis-match:
	Boot into provisioning OS
	$ cat /boot/efi/EFI/grub2/test_os.d/0130_rhel9.3-x86_64.cfg
	$ lsblk
	$ mount /dev/sdb3 /mnt
	$ mount /dev/sdb1 /mnt/boot/efi
	$ ls -la /mnt/boot/
	$ cat /mnt/etc/*release
	
	Don't forget to unmount:
		umount /dev/sdb1
		umount /dev/sdb3
		
Manual's section numbers and the types of pages man page contain:
		1. Executable programs or shell commands
		2. System calls (functions provided by the kernel)
		3. Library calls (functions within program libraries)
		4. Special files (usually found in /dev)
		
watch -n 1 -d netstat -s

To check userland RDMA libraries / RDMA core (libibverbs) on Ubuntu run: ibv_devinfo

To handle when it prompts for RSA fingerprint authentication:
	StrictHostKeyChecking=no (ssh -o StrictHostKeyChecking=no -l username hostname "pwd; ls")

To run script on Machine B from Machine A where script is at Machine A:
	ssh user@<machineb> 'bash -s' < </path/machinea>/script.sh
To run script on Machine B from Machine A where script is at Machine B:
	ssh user@<machineb> 'bash -s < </path/machinea>/script.sh'
	
To run multiple command on multiple host, remotely:
	for host in dellr7615g dellr7625b; do ssh $host "whoami; uptime"; done

To install QEMU/virt tools:
	sudo apt install qemu-kvm libvirt-daemon-system libvirt-clients bridge-utils virt-manager

WSL (https://learn.microsoft.com/en-us/windows/wsl/install):
	wsl --install -d Debian
	Reboot the system (After reboot install "Windows Terminal" from start -> Microsoft store) (open start -> Terminal and update settings -> "Default profile" to Debian)
	sudo apt install build-essential
	sudo apt install vim
	sudo apt install git
	
	copy "id_rsa" (private) to ~/.ssh/
	chmod 0400 id_rsa
	
	git config --global user.email "prasant.kumar@amd.com"
	git config --global user.name "Prasant Kumar"
	
	
To get man pages install:
	man-db
	manpages
	manpages-dev

make variables:
	out.o: src.c src.h
	  $@   # "out.o" (target)
	  $<   # "src.c" (first prerequisite)
	  $^   # "src.c src.h" (all prerequisites)
	 
	%.o: %.c
	  $*   # the 'stem' with which an implicit rule matches ("foo" in "foo.c")
	 
	also:
	  $+   # prerequisites (all, with duplication)
	  $?    # prerequisites (new ones)
	  $|    # prerequisites (order-only?)
	 
	  $(@D) # target directory
  
 

journalctl:
	timedatectl list-timezones
	sudo timedatectl set-timezone <zone>
	timedatectl status
	since the most recent reboot: journalctl -b
	/etc/systemd/journald.conf
	journalctl --list-boots
	journalctl --since "2015-01-10 17:15:00"
	journalctl --since "2015-01-10" --until "2015-01-11 03:00"
	journalctl --since yesterday
	journalctl --since 09:00 --until "1 hour ago"
	journalctl -u nginx.service
	journalctl -u nginx.service --since today
	journalctl -u nginx.service -u php-fpm.service --since today
	journalctl _PID=8088
	journalctl _UID=33 --since today
	journalctl -F _GID
	journalctl /usr/bin/bash
	journalctl -k (--dmesg)
	journalctl -k -b -5
	journalctl -p err -b
		0: emerg
		1: alert
		2: crit
		3: err
		4: warning
		5: notice
		6: info
		7: debug
	journalctl --no-full
	journalctl -a
	journalctl -b -u nginx -o json
	journalctl -b -u nginx -o json-pretty
		cat: Displays only the message field itself.
		export: A binary format suitable for transferring or backing up.
		json: Standard JSON with one entry per line.
		json-pretty: JSON formatted for better human-readability
		json-sse: JSON formatted output wrapped to make add server-sent event compatible
		short: The default syslog style output
		short-iso: The default format augmented to show ISO 8601 wallclock timestamps.
		short-monotonic: The default format with monotonic timestamps.
		short-precise: The default format with microsecond precision
		verbose: Shows every journal field available for the entry, including those usually hidden internally.
	journalctl -n 20
	journalctl -f
	sudo journalctl --system -f (To see kernel logs)
	journalctl --disk-usage
	sudo journalctl --vacuum-size=1G
	sudo journalctl --vacuum-time=1years
	
SSH:
	To connect to foo.eng.domainname.com as the user me configure into $HOME/.ssh/config:
		Host *.eng
			Hostname %h.domainname.com
			User me
	To append domainname.com to any host that has at most 1 dot in its name configure into $HOME/.ssh/config:
		CanonicalizeHostname yes
		CanonicalDomains domainname.com

To schedule a command 		:
	echo "command_to_execute" | at HH:MM
		 echo "ls -l" | at 15:30
	echo "command_to_execute" | at YYYY-MM-DD HH:MM
		echo "my_script.sh" | at 2025-03-20 08:00
	echo "command_to_execute" | at now + <number> <units>
		 echo "another_script.sh" | at now + 30 minutes
	To run the commands in script.sh at the specified time
		 at -f script.sh HH:MM
	To lists all pending at jobs
		atq
	Removing scheduled jobs:
		atrm <job_number>
	To check atd status and start
		systemctl status atd
		systemctl start atd

To use UART:
	Install minicom: yum install minicom
	Configure /dev/ttyUSB*: sudo minicom -s
		Select "Serial port setup" hit enter
		Click "A" to update "Serial Device" from /dev/modem to /dev/ttyUSB* then hit enter
		Click "F" to update "Hardware Flow Control" from "Yes" to "No" then hit enter to go back to main menu
		Select "Save setup as" and give a name "elba_uart" then hit enter to get into saved name execute "sudo minicom elba_uart"
	Inside UART login: root:pen123
	Bringup mgmt interface: pdsctl debug update port --port eth1/3 --admin-state up
	Check the port is up: pdsctl show port status
	Notedown the oob_mnic0 IP address to login through DUT to update FW: 10.108.206.204
	To exit from minicom press ctrl a then z which will give you the options then hit "x"
	
To use ymodem to send file from inside UART: ctrl A-Z , S, ymodem, select zephyr.fit file (zephyr.fit should be at /root/ from where minicom has been started)

################################ Debian / Ubuntu ################################################
dpkg -c ibverbs-utils_54.0-1_amd64.deb
dpkg -I ibverbs-utils_54.0-1_amd64.deb
dpkg -S /lib/x86_64-linux-gnu/libionic.so.1.0.54.0
 ls -1 *.deb | xargs -n1 dpkg -c | less
apt-file search libionic.so.1.0.54.0

############################################## lspci #############################
To see the tree view:
	# lspci  -tv

To see root complex of device:
	# lspci -PP -d 1dd8:

To see the capability of root complex:
	# lspci -s c0:01.1 -vvv | grep Lnk

To see the capability of the device:
	# lspci -PP -d 1dd8: -vvv | grep Lnk

############################################## apt #################################
To check installed packages:
	root@ubuntu:/# apt list *rocm* --installed

############################################## C ###############################
For C code:
	#!/bin/bash

	set -ex

	find . -name '*.[ch]' -type f > cscope.files
	ctags -L cscope.files
	cscope -q

	exit 0


###################### Pollara package #########################################
Pre-requisite Packages from linux / Ubuntu / ORCL / CentOS / debian

       iperf3
       scapy     ## install under root creds using "pip3 install scapy"  (or)  apt install python3-pip  python3-scapy
       tcpdump
       ethtool
       rdma
       policycoreutils
       docker-compose
       pkg-config
       linux-headers-$(uname -r)       ## ( if missing ) 
       libnl-route-3-dev
       net-tools            ## ( if missing ) 
       stress-ng
       psutil    ###  using pip3 install psutil 

################################################ tmux – Terminal Multiplexer ###########################################
Key Features:
	Split your terminal into multiple panes (horizontally or vertically).
	Detach and reattach sessions, ensuring your work persists even if your connection drops.
	Customizable key bindings for quick navigation.

To install tmux on Linux, run:
	sudo apt install tmux         [On Debian, Ubuntu and Mint]
	sudo dnf install tmux         [On RHEL/CentOS/Fedora and Rocky/AlmaLinux]
	sudo emerge -a sys-apps/tmux  [On Gentoo Linux]
	sudo apk add tmux             [On Alpine Linux]
	sudo pacman -S tmux           [On Arch Linux]
	sudo zypper install tmux      [On OpenSUSE]    
	sudo pkg install tmux         [On FreeBSD]
	
start a new session:
	tmux new -s session_name
	
split panes:
	Ctrl+b % (vertical)
	Ctrl+b " (horizontal)
	
detach:
	Ctrl+b d

################################################ htop – Interactive Process Viewer ###########################################
Key Features:
	Color-coded display for easy readability.
	Scrollable process list and tree view.
	Kill or renice processes directly from the interface.

To install htop on Linux, run:
	sudo apt install htop         [On Debian, Ubuntu and Mint]
	sudo dnf install htop         [On RHEL/CentOS/Fedora and Rocky/AlmaLinux]
	sudo emerge -a sys-apps/htop  [On Gentoo Linux]
	sudo apk add htop             [On Alpine Linux]
	sudo pacman -S htop           [On Arch Linux]
	sudo zypper install htop      [On OpenSUSE]    
	sudo pkg install htop         [On FreeBSD]

Once installed, simply type htop in your terminal to launch the tool and use the arrow keys to navigate, and press F9 to kill a process.

################################################ fzf – Fuzzy Finder ###########################################
Key Features:
	Fuzzy matching for quick searches.
	Works with command history, file paths, and more.
	Integrates seamlessly with other tools like vim, bash, and zsh.
	Highly customizable and scriptable.

To install fzf on Linux, run:
	sudo apt install fzf         [On Debian, Ubuntu and Mint]
	sudo dnf install fzf         [On RHEL/CentOS/Fedora and Rocky/AlmaLinux]
	sudo emerge -a sys-apps/fzf  [On Gentoo Linux]
	sudo apk add fzf             [On Alpine Linux]
	sudo pacman -S fzf           [On Arch Linux]
	sudo zypper install fzf      [On OpenSUSE]    
	sudo pkg install fzf         [On FreeBSD]

Once installed, run fzf in your terminal to start searching. Combine it with commands like ls | fzf to filter file lists.

################################################ bat – Enhanced Cat Command ###########################################
Key Features:
	Syntax highlighting for over 200 programming languages.
	Line numbers and Git diff support.
	Paging support for large files.

To install bat on Linux, run:
	sudo apt install bat         [On Debian, Ubuntu and Mint]
	sudo dnf install bat         [On RHEL/CentOS/Fedora and Rocky/AlmaLinux]
	sudo emerge -a sys-apps/bat  [On Gentoo Linux]
	sudo apk add bat             [On Alpine Linux]
	sudo pacman -S bat           [On Arch Linux]
	sudo zypper install bat      [On OpenSUSE]    
	sudo pkg install bat         [On FreeBSD]

Once installed, just replace cat with bat. For example, bat filename.txt displays the file with syntax highlighting. If bat is not available, try batcat instead.

################################################ eza – Modern Alternative to ls ###########################################
Key Features:
	Color-coded output for better readability.
	Displays file metadata like permissions, size, and modification time.
	Tree view for hierarchical directory structures.

To install eza on Linux, run:
	sudo apt install eza         [On Debian, Ubuntu and Mint]
	sudo dnf install eza         [On RHEL/CentOS/Fedora and Rocky/AlmaLinux]
	sudo emerge -a sys-apps/eza  [On Gentoo Linux]
	sudo apk add eza             [On Alpine Linux]
	sudo pacman -S eza           [On Arch Linux]
	sudo zypper install eza      [On OpenSUSE]    
	sudo pkg install eza         [On FreeBSD]

Once installed, use eza instead of ls. For example, eza -l provides a detailed listing, and eza --tree shows a directory tree.

################################################ ripgrep (rg) – Fast File Search ###########################################
Key Features:
	Recursively searches directories.
	Ignores files and directories specified in .gitignore.
	Supports regex and Unicode.

To install ripgrep on Linux, run:
	sudo apt install ripgrep         [On Debian, Ubuntu and Mint]
	sudo dnf install ripgrep         [On RHEL/CentOS/Fedora and Rocky/AlmaLinux]
	sudo emerge -a sys-apps/ripgrep  [On Gentoo Linux]
	sudo apk add ripgrep             [On Alpine Linux]
	sudo pacman -S ripgrep           [On Arch Linux]
	sudo zypper install ripgrep      [On OpenSUSE]    
	sudo pkg install ripgrep         [On FreeBSD]

Once installed, run rg "search_term" to find occurrences of the term in your project directory.

################################################ Zsh – Z Shell ###########################################
Key Features:
	Advanced tab completion.
	Plugin ecosystem via frameworks like Oh My Zsh.
	Themes for customizing the prompt.

To install Zsh on Linux, run:
	sudo apt install zsh         [On Debian, Ubuntu and Mint]
	sudo dnf install zsh         [On RHEL/CentOS/Fedora and Rocky/AlmaLinux]
	sudo emerge -a sys-apps/zsh  [On Gentoo Linux]
	sudo apk add zsh             [On Alpine Linux]
	sudo pacman -S zsh           [On Arch Linux]
	sudo zypper install zsh      [On OpenSUSE]    
	sudo pkg install zsh         [On FreeBSD]

Once installed, change your default shell to Zsh with chsh -s $(which zsh)

################################################ Git – Version Control System ###########################################
Key Features:
	Branching and merging for parallel development.
	Distributed version control.
	Integration with platforms like GitHub and GitLab.

To install git on Linux, run:
	sudo apt install git         [On Debian, Ubuntu and Mint]
	sudo dnf install git         [On RHEL/CentOS/Fedora and Rocky/AlmaLinux]
	sudo emerge -a sys-apps/git  [On Gentoo Linux]
	sudo apk add git             [On Alpine Linux]
	sudo pacman -S git           [On Arch Linux]
	sudo zypper install git      [On OpenSUSE]    
	sudo pkg install git         [On FreeBSD]

initialize a repository:
	git init
add files:
	git add .
commit changes:
	git commit -m "message"
push to a remote repository:
	git push


################################################ cURL – Data Transfer Tool ###########################################
Key Features:
	Supports multiple protocols.
	Can send HTTP requests with headers and data.
	Works with both GET and POST requests.

To install cURL on Linux, run:
	sudo apt install curl         [On Debian, Ubuntu and Mint]
	sudo dnf install curl         [On RHEL/CentOS/Fedora and Rocky/AlmaLinux]
	sudo emerge -a sys-apps/curl  [On Gentoo Linux]
	sudo apk add curl             [On Alpine Linux]
	sudo pacman -S curl           [On Arch Linux]
	sudo zypper install curl      [On OpenSUSE]    
	sudo pkg install curl         [On FreeBSD]

fetch a webpage:
	curl http://example.com
	curl google.com

send a POST request:
	curl -X POST -d "param=value" http://example.com

################################################ tldr – Simplified Man Pages ###########################################
Key Features:
	Easy-to-understand examples.
	Available offline after initial setup.
	Covers a wide range of commands.

To install tldr on Linux, run:
	sudo apt install tldr         [On Debian, Ubuntu and Mint]
	sudo dnf install tldr         [On RHEL/CentOS/Fedora and Rocky/AlmaLinux]
	sudo emerge -a sys-apps/tldr  [On Gentoo Linux]
	sudo apk add tldr             [On Alpine Linux]
	sudo pacman -S tldr           [On Arch Linux]
	sudo zypper install tldr      [On OpenSUSE]    
	sudo pkg install tldr         [On FreeBSD]

Once installed, type tldr command_name to get quick usage examples. For instance, tldr tar explains common tar operations.

################################################ jq – JSON Processor ###########################################
Key Features:
	Extract and transform JSON data.
	Supports complex queries and transformations.
	Easy to integrate into scripts.

To install jq on Linux, run:
	sudo apt install jq         [On Debian, Ubuntu and Mint]
	sudo dnf install jq         [On RHEL/CentOS/Fedora and Rocky/AlmaLinux]
	sudo emerge -a sys-apps/jq  [On Gentoo Linux]
	sudo apk add jq             [On Alpine Linux]
	sudo pacman -S jq           [On Arch Linux]
	sudo zypper install jq      [On OpenSUSE]    
	sudo pkg install jq         [On FreeBSD]

run it for processing JSON data:
	cat file.json | jq
	

################################################ ncdu – Disk Usage Analyzer ###########################################
Key Features:
	Interactive interface for navigating directories.
	Sort files by size for quick identification.
	Lightweight and fast compared to graphical alternatives like Baobab.

To install ncdu on Linux, run:
	sudo apt install ncdu         [On Debian, Ubuntu and Mint]
	sudo dnf install ncdu         [On RHEL/CentOS/Fedora and Rocky/AlmaLinux]
	sudo emerge -a sys-apps/ncdu  [On Gentoo Linux]
	sudo apk add ncdu             [On Alpine Linux]
	sudo pacman -S ncdu           [On Arch Linux]
	sudo zypper install ncdu      [On OpenSUSE]    
	sudo pkg install ncdu         [On FreeBSD]

Once installed, run ncdu /path/to/directory to analyze disk usage.
Navigate with arrow keys, and delete files directly from the interface by pressing d.
You can also refresh the view with r or quit with q.


################################################ asciinema – Terminal Session Recorder ###########################################
Key Features:
	Records input and output without capturing sensitive data.
	Shareable links for collaboration and documentation.
	Playback can be paused, rewound, and copied.

To install asciinema on Linux, run:
	sudo apt install asciinema         [On Debian, Ubuntu and Mint]
	sudo dnf install asciinema         [On RHEL/CentOS/Fedora and Rocky/AlmaLinux]
	sudo emerge -a sys-apps/asciinema  [On Gentoo Linux]
	sudo apk add asciinema             [On Alpine Linux]
	sudo pacman -S asciinema           [On Arch Linux]
	sudo zypper install asciinema      [On OpenSUSE]    
	sudo pkg install asciinema         [On FreeBSD]

start recording:
	asciinema rec
stop recording:
	Ctrl+D
save the recording locally by specifying a filename:
	asciinema rec demo.cast
To play back a local recording:
	asciinema play demo.cast.

##################################################### Creating bridge0 #################################################
ip link add bridge0 type bridge
ip link set dev enp199s0 up
ip link show dev enp199s0
	14: enp199s0: <BROADCAST,MULTICAST,UP,LOWER_UP> mtu 1500 qdisc mq state UP mode DEFAULT group default qlen 1000
		link/ether 04:90:81:2b:a2:08 brd ff:ff:ff:ff:ff:ff
ip link set bridge0 address 04:90:81:2b:a2:08
ip link set enp199s0 master bridge0
ip -d link show
	15: bridge0: <BROADCAST,MULTICAST> mtu 1500 qdisc noop state DOWN mode DEFAULT group default qlen 1000
		link/ether 04:90:81:2b:a2:08 brd ff:ff:ff:ff:ff:ff promiscuity 0 minmtu 68 maxmtu 65535 
ip link set bridge0 up
/sbin/ip address add dev bridge0 local 192.168.131.106/21 broadcast 192.168.135.255
/sbin/ip address add dev bridge0 local fd00::200:c0ff:fea8:836a/117
/sbin/ip -d address show
	15: bridge0: <BROADCAST,MULTICAST,UP,LOWER_UP> mtu 1500 qdisc noqueue state UP group default qlen 1000
		link/ether 04:90:81:2b:a2:08 brd ff:ff:ff:ff:ff:ff promiscuity 0 minmtu 68 maxmtu 65535 
		bridge forward_delay 1500 hello_time 200 max_age 2000 ageing_time 30000 stp_state 0 priority 32768 vlan_filtering 0 vlan_protocol 802.1Q bridge_id 8000.4:90:81:2b:a2:8 designated_root 8000.4:90:81:2b:a2:8 root_port 0 root_path_cost 0 topology_change 0 topology_change_detected 0 hello_timer    0.00 tcn_timer    0.00 topology_change_timer    0.00 gc_timer  299.91 vlan_default_pvid 1 vlan_stats_enabled 0 vlan_stats_per_port 0 group_fwd_mask 0 group_address 01:80:c2:00:00:00 mcast_snooping 1 mcast_router 1 mcast_query_use_ifaddr 0 mcast_querier 0 mcast_hash_elasticity 16 mcast_hash_max 4096 mcast_last_member_count 2 mcast_startup_query_count 2 mcast_last_member_interval 100 mcast_membership_interval 26000 mcast_querier_interval 25500 mcast_query_interval 12500 mcast_query_response_interval 1000 mcast_startup_query_interval 3124 mcast_stats_enabled 0 mcast_igmp_version 2 mcast_mld_version 1 nf_call_iptables 0 nf_call_ip6tables 0 nf_call_arptables 0 numtxqueues 1 numrxqueues 1 gso_max_size 65536 gso_max_segs 65535 
		inet 192.168.131.106/21 brd 192.168.135.255 scope global bridge0
		   valid_lft forever preferred_lft forever
		inet6 fd00::200:c0ff:fea8:836a/117 scope global tentative 
		   valid_lft forever preferred_lft forever
		inet6 fe80::690:81ff:fe2b:a208/64 scope link tentative 
		   valid_lft forever preferred_lft forever
ip -all netns exec ip -d address show


#############################################################################################################################
Websites to follow:
	https://sqlitebrowser.org/
	https://github.com/features/codespaces
	
Run the following commands to create a user vm and grant it root privileges:
	sudo useradd -m -s /bin/bash vm
	sudo passwd vm  # Set a password for the user
	sudo usermod -aG sudo vm  # Grant sudo privileges
	echo "vm ALL=(ALL) NOPASSWD:ALL" | sudo tee /etc/sudoers.d/vm

To reset the USB driver:
	sudo udevadm control --reload
	sudo udevadm trigger
	
##################################################### SRIOV ############################################
SR-IOV Virtual Functions (VFs) can be assigned to virtual machines by adding a device entry in <hostdev> with the `virsh edit` or `virsh attach-device` command.

To attach an SR-IOV network device on an Intel or an AMD system:
	Enable Intel VT-d or the AMD IOMMU specifications in the BIOS and kernel.
		https://docs.redhat.com/en/documentation/red_hat_enterprise_linux/6/html/virtualization_host_configuration_and_guest_installation_guide/chap-Virtualization_Host_Configuration_and_Guest_Installation_Guide-PCI_Device_Config#sect-Virtualization_Host_Configuration_and_Guest_Installation_Guide-PCI_Assignment-AI_device_to_a_host
		
		Enable the AMD IOMMU specifications:
			The AMD IOMMU specifications are required to use PCI device assignment in Red Hat Enterprise Linux. 
			These specifications must be enabled in the BIOS. Some system manufacturers disable these specifications by default.
			
		Enable IOMMU kernel support:
			Append amd_iommu=on to the kernel command line in /boot/grub/grub.conf so that AMD IOMMU specifications are enabled at boot.
			
		Ready to use:
			Reboot the system to enable the changes. Your system is now capable of PCI device assignment.
	
	Verify support:
		`lspci`??
	
	Start the SR-IOV kernel modules:
		# `modprobe <driver> [<option>=<VAL1>,<VAL2>,]` ??
		
	Activate Virtual Functions: The `max_vfs` parameter causes the driver to spawn, up to the value of the parameter in, Virtual Functions.
		Remove the module to change the variable:
			# modprobe -r <driver>
		Restart the module with the `max_vfs` set to any number of Virtual Functions or up to the maximum supported by your device.
			# modprobe <driver> max_vfs=<num>
	
	Make the Virtual Functions persistent:
		# echo "options ,driver> max_vfs=7" >>/etc/modprobe.d/<driver>.conf ??
	
	Inspect the new Virtual Functions:
		# lspci | grep "Virtual Function"
	
	Verify devices exist with virsh:
		
	
	Assigning a PCI Device with virsh:
		Identify the device:
			lspci | grep Ethernet
		Find out the full identifier:
			sudo virsh nodedev-list --cap pci
		Review device information:
			sudo virsh nodedev-dumpxml pci_0000_c7_00_0
		Determine required configuration details: From the output of above command
			Convert bus, slot and function values into  hexadecimal.
		Add configuration details:
			Run `virsh edit`, specifying the virtual machine name, and add a device entry in the <source> section to assign the PCI device to the guest virtual machine.
				# virsh edit guest1-rhel6-64
				<hostdev mode='subsystem' type='pci' managed='yes'>
				  <source>
					 <address domain='0x0' bus='0x0' slot='0x19' function='0x0'/>
				  </source>
				</hostdev>
			Alternately, run `virsh attach-device`, specifying the virtual machine name and the guest's XML file:
				virsh attach-device guest1-rhel6-64 file.xml
		Allow device management:
			Set an SELinux boolean to allow the management of the PCI device from the virtual machine:
				# setsebool -P virt_use_sysfs 1
		Start the virtual machine:
			# virsh start guest1-rhel6-64
	
#################################################################### Install DKMS ####################################################
	1. check the list of kernels in the system

	# dnf repoquery --installonly
	kernel-0:6.4.3-0_fbk15_zion_2630_gf27365f948db.x86_64
	kernel-core-0:5.14.0-547.el9.x86_64
	kernel-core-0:5.14.0-553.el9.x86_64
	kernel-modules-0:5.14.0-547.el9.x86_64
	kernel-modules-0:5.14.0-553.el9.x86_64
	kernel-modules-core-0:5.14.0-547.el9.x86_64
	kernel-modules-core-0:5.14.0-553.el9.x86_64

	2. remove the ones that are not required. For hydra runs, we only need the fbk kernel

	# dnf remove kernel-core-0:5.14.0-547.el9.x86_64 kernel-core-0:5.14.0-553.el9.x86_64 kernel-modules-0:5.14.0-547.el9.x86_64 kernel-modules-0:5.14.0-553.el9.x86_64 kernel-modules-core-0:5.14.0-547.el9.x86_64 kernel-modules-core-0:5.14.0-553.el9.x86_64

	3. create the grub config again, it will prune all the other entries from grub menu.

	#grub2-mkconfig -o /etc/default/grub


	4. reboot the host once to make sure the grub menu doesnt have other entries and booting directly into the fbk kernel

	5. install dkms

	# dnf install dkms

	6. proceed with install.sh - it should work.
###########################################################################################################################################################
Debian:
-----------
Build the deb package: dpkg-deb --build --root-owner-group <helloworld_1.0-1_arm64>
Test your deb package: sudo dpkg -i <package>
remove the package: sudo dpkg -r <appname>
remove it along with the configuration files (if any): sudo dpkg -P <appname>
Make sure the application has been removed correctly by issuing: dpkg -l | grep <appname>
deb installation goes wrong with (Package is in a very bad inconsistent state - you should reinstall it before attempting a removal.):
	sudo mv /var/lib/dpkg/info/<packagename>.* /tmp/
	sudo dpkg --remove --force-remove-reinstreq <packagename>

#############################################################################################################################################################
Compile a Linux Kernel:
1. wget https://git.kernel.org/torvalds/t/linux-4.17-rc2.tar.gz
2. sudo apt-get install git fakeroot build-essential ncurses-dev xz-utils libssl-dev bc flex libelf-dev bison cmake clang llvm
3. tar xvzf linux-4.17-rc2.tar.gz
4. cd linux-4.17-rc2
5. cp /boot/config-$(uname -r) .config
6. make menuconfig
7. make clean -j 8
8. make -j 8
9. make modules
10. make headers_install
11. make M=samples/bpf

####################################################################################################################################################################

